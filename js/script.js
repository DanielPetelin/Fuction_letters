(function(){
    var laters = {},
        inp = document.getElementById("input-field"),
        val,
        oldVal,
        lastChar;
    inp.addEventListener("keyup", function(e){
        var = this.value;
        if (e.keyCode === 8) {

        } else if (e.keyCode) {

        }
    });
})();